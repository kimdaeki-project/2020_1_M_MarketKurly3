<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
   <mapper namespace="com.iu.mk.review.reviewfile.ReviewFileDAO">
   		<insert id="fileInsert" parameterType="ReviewFileVO">
   			insert into reviewFile values (R_FILENUM_SEQ.nextval, #{review_num},#{oriName},#{fileName})
   		</insert>
   
 	
   </mapper>